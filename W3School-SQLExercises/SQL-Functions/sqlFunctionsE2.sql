/**
 *	from SQL Functions: Exercise 2 ( https://www.w3schools.com/sql/exercise.asp?filename=exercise_functions2 )
 *
 *	question:
 *	Use an SQL function to select the record with the highest value of the Price column.
 *
 *	SELECT __________
 *	FROM Products;
 *
 */
SELECT MAX(Price)
FROM Products;