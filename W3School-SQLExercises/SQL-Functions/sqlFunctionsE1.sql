/**
 *	from SQL Functions: Exercise 1 ( https://www.w3schools.com/sql/exercise.asp?filename=exercise_functions1 )
 *
 *	question:
 *	Use the MIN function to select the record with the smallest value of the Price column.
 *
 *	SELECT __________
 *	FROM Products;
 *
 */
SELECT MIN(Price)
FROM Products;