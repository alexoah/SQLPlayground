/**
 *	from SQL Alias: Exercise 2 ( https://www.w3schools.com/sql/exercise.asp?filename=exercise_alias2 )
 *
 *	question:
 *	When displaying the Customers table, refer to the table as Consumers instead of Customers.
 *
 *	SELECT *
 *	FROM Customers ____________;
 *
 */
SELECT *
FROM Customers AS Consumers;